/*
 * Click nbfs://nbhost/SystemFileSystem/Templates/Licenses/license-default.txt to change this license
 * Click nbfs://nbhost/SystemFileSystem/Templates/Classes/Class.java to edit this template
 */
package com.casalimpia_app.cruce_datos_excel.controller;

/*
import com.casalimpia_app.cruce_datos_excel.util.FileChooserUtil;
import com.casalimpia_app.cruce_datos_excel.model.FileProcessor;
import javafx.event.ActionEvent;
*/
import javafx.fxml.FXML;
import javafx.scene.control.Button;
//import javafx.stage.Stage;

/*
import java.io.File;
import java.io.IOException;
import org.apache.poi.EncryptedDocumentException;
import org.apache.poi.openxml4j.exceptions.InvalidFormatException;
/*


/**
 *
 * @author jcavilaa
 */
public class MainController {

    @FXML
    public static Button processButton;
    
    /*
    @FXML
    private void handleProcessButtonAction(ActionEvent event) throws EncryptedDocumentException, IOException, InvalidFormatException {
        Stage stage = (Stage) processButton.getScene().getWindow();
       
        // Abrir explorador de archivos para seleccionar el archivo a procesar
        File selectedFile = FileChooserUtil.openFileChooser(stage);
       
        if (selectedFile != null) {
            // Procesar el archivo Excel
            FileProcessor processor = new FileProcessor();
            processor.processExcelFile(selectedFile, outputFilePath);   

            // Abrir explorador para guardar el archivo procesado
            File saveFile = FileChooserUtil.openSaveFileChooser(stage);
           
            if (saveFile != null) {
                processor.saveProcessedFile(saveFile);
            }
        }
    }*/
}
